# Підготовка до блоку 12 — 25.09.2026

## Межі висновку

Погоджену підготовку завершено для продовження розробки блоку 12. Це не
підтвердження готовності тестової бази до реального річного закриття чи офіційного
податкового подання. Залишаються історичні зауваження щодо походження записів.

## Виправлення локальних даних

Компанія 1, звірка 01.01–31.12.2026, схема `d1552cd41bf1`.
У документах 1 (`REC-0001`, 1 255 грн), 5 (`REC-0002`, 650 грн) та їхніх
журналах 8 і 9 відновлено `accounting_rule_id = 1` (`PURCHASE_RECEIPT`).
Перед зміною перевірено реквізити, рахунки й суми незалежно від опису журналу;
цільові записи заблоковано, резервну копію записано до зміни, результат звірено
до фіксації транзакції. Змінено лише чотири посилання. Суми, дати, складські рухи,
проводки та ПДВ не змінювалися.

Кредиторка й склад: очікувана сума **1 905,00**, сума в GL **1 905,00**, різниця
**0,00 грн**. Різниця також нульова в дебіторці, касі/банку, вхідному та вихідному ПДВ.
Залишок FIFO: 15 одиниць, 1 905 грн.

Користувач підтвердив, що обидва надходження тестові. Реальні постачальники,
договори та борги за ними не створювалися. Ці тестові залишки не є готовим
перенесенням реальної кредиторки за контрагентами.

## Погоджене очищення чернетки

Після резервного копіювання та окремої згоди користувача через штатний API
видалено лише непроведений журнал №2 `TEST-JE-UNBALANCED-001` від 18.08.2026
та його рядки №3/4 (Дт 1 000, Кт 900). Повторно перевірено відсутність зовнішніх
посилань і точну відповідність резервній копії. У компанії 1 непроведених
бухгалтерських журналів більше немає. Складська чернетка №2 — інший документ;
це погодження не поширюється на неї.

## Історичні зауваження, які залишаються видимими

AP та inventory мають статус `mismatch`, по 8 діагностичних зауважень кожен,
попри нульову сумову різницю. Інші чотири напрями мають статус `matched`.

- Сторновані списання №3 `ISS-0002` (−7/+7 одиниць) і №7 `CONC-ISS-002`
  (−6/+6) не мають відновлюваного бухгалтерського джерела. На кінець
  перевіреного періоду кількість компенсована; це не підтверджує правильність
  кожної історичної дати, особливо списання 17.08 і сторнування 18.08 для №3.
- Ручні тестові журнали №1/5 (1 000 грн) та №6/7 (1 500 грн) утворюють
  пари проведення/сторнування з нульовим підсумком, але без документального джерела.

Зауваження розібрано й пояснено, але відсутнє походження не відновлено штучно.
Проведені записи не видалялися, правила контролю не послаблювалися. Перед
використанням бази для реального обліку потрібне окреме рішення щодо тестової історії
та перенесення підтверджених реальних залишків.

## Репетиція закриття

`scripts/verify_pre12_period_copy.py` — спеціальна перевірка цього локального
набору: компанія 1, серпень 2026. Не є універсальною командою міграції.
Вона створює тимчасову схему з усіма 104 таблицями ORM (850 рядків), власними
послідовностями й відновленими зовнішніми ключами. Типи enum використовуються
з `public` лише як типи; пошук таблиць обмежено схемою копії. Копія видаляється
у `finally`. Запускати звичайним Python без `-O` (перевірки використовують assert).

Перевірено:

1. Збіг відбитків даних усіх 104 таблиць оригіналу й початкової копії.
2. Закриття серпня та відхилення повторного закриття.
3. Відхилення спроби проведення в закритий період.
4. Відкриття й повторне закриття періоду.
5. Незмінність головної книги, ОСВ та карток 281/631; збіг оборотів GL і ОСВ.
6. Нульові сумові різниці всіх шести звірок зі збереженням історичних зауважень.
7. Незмінність даних усіх 104 вихідних таблиць після репетиції.

Робочий серпень залишається відкритим; реальний рік не закривався.

## Приватні докази

Резервні копії та результати збережені локально, поза Git:
`receipt-rule-repair-before.json`, `audit-1905-applied.json`, `draft-2-before.json`,
`pre12-period-copy-result.json` у робочій папці задачі Codex.
Виправлення застосовані до локальної бази; push документації та перевірочного
скрипту не змінює даних в інших інсталяціях.

## Перевірки коду

Цільовий запуск із PostgreSQL: **22 passed**, 172 попередження про застарілі API,
78,13 с; пропущених або невдалих тестів немає:

```bash
PYTHONDONTWRITEBYTECODE=1 RUN_POSTGRES_E2E=1 .venv/bin/python -m pytest -q \
  -p no:cacheprovider tests/test_accounting_period_hardening.py \
  tests/test_accounting_controls.py tests/test_phase11_stabilization_postgresql.py
```

Попередня повна регресія на `e6fbbf8`: 3557 passed (24.09.2026). У цьому
оновленні бізнес-код і схема не змінювалися; повну регресію повторно не запускали.
`git diff --check` пройшов. Контроль робочої бази після репетиції підтвердив:
тимчасових схем немає, серпень відкритий і не заблокований, чернеток журналів 0,
незбалансованих/порожніх проведених журналів компанії 1 немає, проведених рядків
на рахунки іншої компанії немає.
